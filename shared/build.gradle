plugins {
    alias libs.plugins.shadow
}

dependencies {
    implementation libs.jedis.get()

    implementation libs.gson.get()
    implementation libs.apacheCommons.get()
    implementation libs.googleCollections.get()
}

tasks {
    assemble {
        dependsOn(shadowJar)
    }
}